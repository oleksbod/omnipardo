{
    "name": "ses-email-lambda",
    "version": "1.0.0",
    "description": "AWS Lambda function for sending emails via Amazon SES",
    "main": "index.js",
    "scripts": {
        "package": "zip -r function.zip index.js package.json node_modules -x '*.git*' '*.zip'"
    },
    "dependencies": {
        "@aws-sdk/client-ses": "^3.490.0"
    }
}
